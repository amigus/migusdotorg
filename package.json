{
  "dependencies": {
    "autoprefixer": "^10.x",
    "postcss": "^8.x",
    "postcss-cli": "^8.0.0",
    "tailwindcss": "^3.x"
  },
  "engines": {
    "node": ">=18.20.4"
  },
  "scripts": {
    "build": "hugo --gc --minify"
  }
}
